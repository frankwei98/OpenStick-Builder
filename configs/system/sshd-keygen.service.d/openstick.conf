[Unit]
# Debian's ssh-keygen -A operation is idempotent. Clear its first-boot-only
# condition so a transient failure can be retried without replacing keys.
ConditionFirstBoot=
ConditionPathIsReadWrite=/etc/ssh
ConditionPathIsSymbolicLink=!/etc/ssh
