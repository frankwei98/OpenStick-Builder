# Optional isolation of auxiliary Qualcomm BAM-DMUX modem channels.
#
# Keep disabled until the board's dev_port mapping has been verified. Interface
# names are deliberately not configured because wwan numbering is not stable.
ENABLED=0
PRIMARY_DEV_PORTS="0"
AUXILIARY_DEV_PORTS="1 2 3 4 5 6 7"
NAMESPACE="openstick-modem-aux"
